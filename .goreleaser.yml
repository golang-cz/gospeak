# See docs at https://goreleaser.com/customization/build
project_name: go2webrpc

builds:
  - main: ./cmd/go2webrpc
    binary: go2webrpc
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - name_template: "{{ .Binary }}.{{ .Os }}-{{ .Arch }}"
    format: binary

checksum:
  name_template: "checksums.txt"

changelog:
  use: github
  sort: asc

brews:
  - name: go2webrpc
    tap:
      owner: golang-cz
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    #homepage: "https://example.com/"
    description: "go2webrpc, Golang interface as your schema for client/server communication"
    license: "MIT"
